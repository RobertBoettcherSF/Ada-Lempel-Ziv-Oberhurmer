project Lzo is
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("main.adb", "tests.adb");

   package Compiler is
      for Switches ("Ada") use ("-gnat2012", "-gnatwa", "-O2");
   end Compiler;
end Lzo;
